import React from 'react';
import { ProfileIcon, SearchIcon } from './';

const Header = () => (
  <div>
    <h2>«Header»</h2>
    <ProfileIcon />
    <SearchIcon />
  </div>
);

export default Header;
